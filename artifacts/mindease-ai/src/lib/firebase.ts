/**
 * Firebase stub — replaced by Supabase.
 * Kept to avoid breaking any residual imports during migration.
 * All actual data operations now use src/lib/supabase.ts
 */
export const auth = null;
export const db   = null;
export const isConfigured = false;
export default null;
